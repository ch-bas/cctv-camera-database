# transient per-run outputs — regenerated by scripts/strix-fetch.js + strix-filter.js
leads-new.json
verify-queue.json
# snapshot cannot be persisted from a read-only CI job yet (needs commit-back or
# actions/cache) — until then every run is a full cold-start sweep. See README.
.snapshot.json
